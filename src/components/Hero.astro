---
import { Image } from 'astro:assets';
import profileImage from '../assets/profile.jpg';
---
<section class="mt-12 mb-20">
  <div class="flex items-center gap-6 mb-8">
    <Image 
      src={profileImage} 
      alt="James Keane" 
      class="w-24 h-24 rounded-full object-cover border-2"
      style="object-position: 80% 50%; border-color: var(--c-border);"
      loading="eager"
    />
    <h1 class="text-4xl md:text-5xl font-serif" style="color: var(--c-text-main);">
      I'm <span class="italic">James Keane</span>.
    </h1>
  </div>
  <p class="text-xl leading-relaxed max-w-2xl font-sans" style="color: var(--c-text-muted);">
    Building applied intelligence systems and agentic workflows. Usually found on a mountain when <span id="offline-trigger" class="cursor-pointer brandur-link">offline</span>.
  </p>
</section>

<script>
  import { isOffline } from '../stores/offline';
  
  const trigger = document.getElementById('offline-trigger');
  trigger?.addEventListener('click', () => {
    isOffline.set(true);
  });
</script>
